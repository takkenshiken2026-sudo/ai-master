擬似言語の記述形式 (エパスポート試験用)
アルゴリズムを表現するための擬似的なプログラム言語 (擬似言語) を使用した問題では;各問
題文中に注記がない限り次の記述形式が適用されているものとする。
〔擬似言語の記述形式〕
記述形式説明
0手続名又は関数名手続又は関数を宣言する。
型名: 整数名変数を宣言する。
ノ 注釈 *|注釈を記述する。
川 注恐
変数名 - 式変数に式の値を代入する。
三続名又は関数名(引数…)手続又は関数を呼び出し,引数を受け渡す。
3・ (条住式1)選沢処理を示す。
処理1条性式を上から評価し, 最初に真になった釜住式に対応す
白[5え1+ (象住式のる処理を実行する。 以降の釜性式は評価せず_対応する処
処理?理も実行しない。どの釜性式も真にならないときは;処理
白5川1+ (象住式』)』土_を実行する。
処理』各処理は;以上の文の集まりである。
白[5巳白[5ァ1・ と処理の組みは; 複数記述することがあり省略す
処理』土】ることもある。
。[らと と処理』土_1の組みはーつだけ記述し,省略するこ
日【小1+
ともある。
ッん;[旦条住式)前判定繰返し処理を示す。
処理条性式が真の間; 処理を繰返し実行する。
旦】ロッん之へと処理は;0以上の文の集まりである。
ロ後判定繰返し処理を示す。
処理処理を実行し, 条性式が真の間;処理を繰返し実行する。
ッん旦条件式)処理は;0以上の文の集まりである。
の「制御記述)繰返し処理を示す。
処理制御記述の内容に基づいて; 処理を繰返し実行する。
日【まィ。7処理は,以上の文の集まりである。
50